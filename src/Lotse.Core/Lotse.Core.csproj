<Project Sdk="Microsoft.NET.Sdk">
  <!-- Pure domain + learning engine. No framework dependencies so it can be unit-tested and reused (CLI, API, WASM). -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Lotse.Core</RootNamespace>
  </PropertyGroup>
</Project>
